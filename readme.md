## Yotta Target Description for AMR on Linux, using Native Runtimes

Use this target to build things using the native compiler and runtimes.

This target can only be used when the host system is running Linux with ARM architecture. For example, raspbian on Raspberry Pi 1 or 2.

```bash
yotta target arm-linux-native
...
yotta install
```
